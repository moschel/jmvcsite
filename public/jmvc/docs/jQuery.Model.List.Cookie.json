C({"name": "jQuery.Model.List.Cookie", "inherits": "jQuery.Model.List", "type": "class", "parent": "../jquery/model/list/cookie/cookie.js", "comment": "<p>Provides a store-able list of model instances.  The following \nretrieves and saves a list of contacts:</p>\n\n<pre><code class='javascript'>var contacts = new Contact.List([]).retrieve(\"contacts\");\n\n// add each contact to the page\ncontacts.each(function(){\naddContact(this);\n});\n\n// when a new cookie is crated\n$(\"#contact\").submit(function(ev){\n    ev.preventDefault();\n    var data = $(this).formParams();\n\n    // gives it a random id\n    data.id = +new Date();\n    var contact = new Contact(data);\n\n    //add it to the list of contacts \n    contacts.push(contact);\n\n    //store the current list\n    contacts.store(\"contacts\");\n\n    //show the contact\n    addContact(contact);\n})</code></pre>\n\n<p>You can see this in action in the following demo.  Create a contact, then\nrefresh the page.</p>\n\n<div class='demo_wrapper' data-demo-src='jquery/model/list/cookie/cookie.html'></div>", "plugin": "jquery/model/list/cookie", "test": "jquery/model/list/cookie/qunit.html", "download": "jquery/dist/jquery.model.list.cookie.js", "downloadSize": 0, "parents": ["jQuery.Model.List"], "children": ["jQuery.Model.List.Cookie.prototype", "jQuery.Model.List.Cookie.prototype.retrieve", "jQuery.Model.List.Cookie.prototype.store"]})